export enum EMethod {
  GET = "GET",
  POST = "POST",
  PATCH = "PATCH",
  DELETE = "DELETE",
}
export enum ESocketNamespace {
  chat = "/chat",
  message = "/messages",
  notification = "/notification",
}
