import { Button } from "antd";

function ButtonNewPost() {
  return (
    <Button type="default" size="large" icon={<i className="fas fa-plus"></i>}>
      New Post
    </Button>
  );
}

export default ButtonNewPost;
